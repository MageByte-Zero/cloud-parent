spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/UniqueDong/cloud-config-rep.git
          username: git账号
          password: 密码
server:
  port: 1201

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1001/eureka/